/**
 * reply-to-both-emails.js
 * Run from your project root:   node reply-to-both-emails.js
 *
 * Confirmed directly in Resend's SDK types that reply-to accepts an
 * array, not just one address. Updated the confirmation emails (the
 * ones customers receive) so replying reaches BOTH
 * tobibamidelejohn98@gmail.com and hello@jaygele.com at once -- whichever
 * one the customer's mail app shows, hitting reply includes both.
 *
 * The notification emails (to Tobi, with the enquiry/booking details)
 * are untouched -- those already reply straight to the customer, which
 * is correct and separate from this.
 */
const fs = require("fs");
const path = require("path");
const root = process.cwd();
function fail(msg) { console.error("Error: " + msg); process.exit(1); }
if (!fs.existsSync(path.join(root, "package.json"))) fail("Run this from your project root.");
function write(rel, content) {
  fs.writeFileSync(path.join(root, rel), content, "utf8");
  console.log("wrote " + rel);
}

write('app/api/contact/route.js', 'import { Resend } from "resend";\nimport { contactNotificationHtml, contactConfirmationHtml } from "@/lib/emailTemplates";\n\n// A simple in-memory rate limiter — resets on server restart. Blocks a burst\n// of spam from the same IP without needing a database. Fine for a site this\n// size; if traffic grows, this would want a proper store (e.g. Redis).\nconst submissions = new Map();\nconst WINDOW_MS = 10 * 60 * 1000; // 10 minutes\nconst MAX_PER_WINDOW = 5;\n\nfunction isRateLimited(ip) {\n  const now = Date.now();\n  const timestamps = (submissions.get(ip) || []).filter((t) => now - t < WINDOW_MS);\n  timestamps.push(now);\n  submissions.set(ip, timestamps);\n  return timestamps.length > MAX_PER_WINDOW;\n}\n\nexport async function POST(request) {\n  try {\n    const ip = request.headers.get("x-forwarded-for")?.split(",")[0]?.trim() || "unknown";\n    if (isRateLimited(ip)) {\n      return Response.json({ error: "Too many messages sent recently. Please try again later." }, { status: 429 });\n    }\n\n    const { name, email, message } = await request.json();\n\n    if (!name || !email || !message) {\n      return Response.json({ error: "Please fill in every field." }, { status: 400 });\n    }\n    const emailPattern = /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/;\n    if (!emailPattern.test(email)) {\n      return Response.json({ error: "Please enter a valid email address." }, { status: 400 });\n    }\n\n    if (!process.env.RESEND_API_KEY || !process.env.CONTACT_TO_EMAIL) {\n      console.error("Contact form: missing RESEND_API_KEY or CONTACT_TO_EMAIL env vars");\n      return Response.json({ error: "The contact form is not fully configured yet. Please email directly instead." }, { status: 500 });\n    }\n\n    const resend = new Resend(process.env.RESEND_API_KEY);\n    const { error } = await resend.emails.send({\n      from: "Gele Glamzzz Website <hello@jaygele.com>",\n      to: process.env.CONTACT_TO_EMAIL,\n      replyTo: email,\n      subject: `New website enquiry from ${name}`,\n      text: `New message from the Gele Glamzzz contact form.\\n\\nName: ${name}\\nEmail: ${email}\\n\\nMessage:\\n${message}`,\n      html: contactNotificationHtml({ name, email, message }),\n    });\n\n    if (error) {\n      console.error("Resend error (contact):", error);\n      return Response.json({ error: "Something went wrong sending your message. Please try again." }, { status: 500 });\n    }\n\n    // The message to the business is the critical part and already\n    // succeeded above. This confirmation to the person who submitted the\n    // form is a nice-to-have on top — if it fails, log it but don\'t turn\n    // an already-successful enquiry into an error for the customer.\n    try {\n      await resend.emails.send({\n        from: "Gele Glamzzz <hello@jaygele.com>",\n        to: email,\n        replyTo: [process.env.CONTACT_TO_EMAIL, "hello@jaygele.com"],\n        subject: "We\'ve received your message — Gele Glamzzz",\n        text: `Hi ${name},\\n\\nThanks for reaching out to Gele Glamzzz. We\'ve received your message and will get back to you shortly.\\n\\nFor your reference, here\'s what you sent:\\n"${message}"\\n\\nSpeak soon,\\nGele Glamzzz`,\n        html: contactConfirmationHtml({ name, message }),\n      });\n    } catch (confirmErr) {\n      console.error("Contact confirmation email failed (business notification already sent):", confirmErr);\n    }\n\n    return Response.json({ success: true });\n  } catch (err) {\n    console.error("Contact form error:", err);\n    return Response.json({ error: "Something went wrong. Please try again." }, { status: 500 });\n  }\n}\n\nfunction escapeHtml(str) {\n  return String(str).replace(/[&<>"\']/g, (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", \'"\': "&quot;", "\'": "&#39;" }[c]));\n}\n');
write('app/api/booking/route.js', 'import { Resend } from "resend";\nimport { bookingNotificationHtml, bookingConfirmationHtml } from "@/lib/emailTemplates";\n\nconst submissions = new Map();\nconst WINDOW_MS = 10 * 60 * 1000;\nconst MAX_PER_WINDOW = 5;\n\nfunction isRateLimited(ip) {\n  const now = Date.now();\n  const timestamps = (submissions.get(ip) || []).filter((t) => now - t < WINDOW_MS);\n  timestamps.push(now);\n  submissions.set(ip, timestamps);\n  return timestamps.length > MAX_PER_WINDOW;\n}\n\nexport async function POST(request) {\n  try {\n    const ip = request.headers.get("x-forwarded-for")?.split(",")[0]?.trim() || "unknown";\n    if (isRateLimited(ip)) {\n      return Response.json({ error: "Too many requests sent recently. Please try again later." }, { status: 429 });\n    }\n\n    const { occasion, dateLabel, serviceTitle, form } = await request.json();\n\n    if (!occasion || !dateLabel || !serviceTitle || !form?.name || !form?.email || !form?.phone || !form?.location) {\n      return Response.json({ error: "Please fill in every required field." }, { status: 400 });\n    }\n    const emailPattern = /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/;\n    if (!emailPattern.test(form.email)) {\n      return Response.json({ error: "Please enter a valid email address." }, { status: 400 });\n    }\n\n    if (!process.env.RESEND_API_KEY || !process.env.CONTACT_TO_EMAIL) {\n      console.error("Booking form: missing RESEND_API_KEY or CONTACT_TO_EMAIL env vars");\n      return Response.json({ error: "The booking form is not fully configured yet. Please email directly instead." }, { status: 500 });\n    }\n\n    const lines = [\n      `Occasion: ${occasion}`,\n      `Requested date: ${dateLabel}`,\n      `Time: ${form.time || "Not specified"}`,\n      `Service: ${serviceTitle}`,\n      `Name: ${form.name}`,\n      `Email: ${form.email}`,\n      `Phone: ${form.phone}`,\n      `Location: ${form.location}`,\n      `Number of people: ${form.people || 1}`,\n      `Notes: ${form.notes || "None"}`,\n    ];\n\n    const resend = new Resend(process.env.RESEND_API_KEY);\n    const { error } = await resend.emails.send({\n      from: "Gele Glamzzz Website <hello@jaygele.com>",\n      to: process.env.CONTACT_TO_EMAIL,\n      replyTo: form.email,\n      subject: `New booking request — ${occasion} — ${form.name}`,\n      text: `New booking request from the Gele Glamzzz website.\\n\\n${lines.join("\\n")}\\n\\nThis is a request, not a confirmed appointment. Reply directly to ${form.email} to confirm.`,\n      html: bookingNotificationHtml({ occasion, dateLabel, serviceTitle, form }),\n    });\n\n    if (error) {\n      console.error("Resend error (booking):", error);\n      return Response.json({ error: "Something went wrong sending your request. Please try again." }, { status: 500 });\n    }\n\n    // Business notification above is the critical part and already\n    // succeeded. This confirmation to the customer is on top of that —\n    // if it fails, log it but don\'t turn a successful request into an\n    // error message for the person who just booked.\n    const customerLines = [\n      `Occasion: ${occasion}`,\n      `Requested date: ${dateLabel}`,\n      `Service: ${serviceTitle}`,\n    ];\n    try {\n      await resend.emails.send({\n        from: "Gele Glamzzz <hello@jaygele.com>",\n        to: form.email,\n        replyTo: [process.env.CONTACT_TO_EMAIL, "hello@jaygele.com"],\n        subject: "We\'ve received your booking request — Gele Glamzzz",\n        text: `Hi ${form.name},\\n\\nThanks for your booking request with Gele Glamzzz. Here\'s a summary of what you sent:\\n\\n${customerLines.join("\\n")}\\n\\nThis is a request, not a confirmed appointment — we\'ll be in touch shortly to confirm availability and finalise the details.\\n\\nSpeak soon,\\nGele Glamzzz`,\n        html: bookingConfirmationHtml({ occasion, dateLabel, serviceTitle, form }),\n      });\n    } catch (confirmErr) {\n      console.error("Booking confirmation email failed (business notification already sent):", confirmErr);\n    }\n\n    return Response.json({ success: true });\n  } catch (err) {\n    console.error("Booking form error:", err);\n    return Response.json({ error: "Something went wrong. Please try again." }, { status: 500 });\n  }\n}\n\nfunction escapeHtml(str) {\n  return String(str).replace(/[&<>"\']/g, (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", \'"\': "&quot;", "\'": "&#39;" }[c]));\n}\n');

console.log("\nDone. Restart the dev server (Ctrl+C, then npm run dev).");
